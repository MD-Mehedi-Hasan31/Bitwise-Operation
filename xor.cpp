#include <iostream>

using namespace std;

int main()
{

    int a,b;
    cin>>a>>b;
    cout<<"Xor operation is: "<<(a^b)<<endl;
    cout<<"OR operation is: "<<(a|b)<<endl;
    cout<<"AND operation is: "<<(a&b)<<endl;

    cout<<"left shift: "<<(5<<);


    return 0;
}
